# Puerto del servicio (por defecto 4001)
AUTH_PORT=4001

# MongoDB — base de datos de usuarios
MONGO_URI=mongodb://localhost:27017/inventario_auth

# Secreto para firmar JWT. Debe ser el MISMO en inventory y reports.
JWT_SECRET=changeme
