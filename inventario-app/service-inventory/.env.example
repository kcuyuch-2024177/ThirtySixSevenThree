# Puerto del servicio (por defecto 4002)
INVENTORY_PORT=4002

# MongoDB — base de datos de productos y movimientos
MONGO_URI=mongodb://localhost:27017/inventario_inventory

# Debe ser EXACTAMENTE el mismo JWT_SECRET que en service-auth
# (este servicio solo verifica tokens, no los emite).
JWT_SECRET=changeme
